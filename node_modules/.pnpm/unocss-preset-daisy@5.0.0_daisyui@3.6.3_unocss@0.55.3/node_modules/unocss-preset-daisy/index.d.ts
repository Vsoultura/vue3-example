import type { Preset } from 'unocss';
declare const defaultOptions: {
    styled: boolean;
    themes: boolean | (string | Record<string, Record<string, string>>)[];
    base: boolean;
    utils: boolean;
    rtl: boolean;
    darkTheme: string;
};
export declare const presetDaisy: (options?: Partial<typeof defaultOptions>) => Preset;
export {};
